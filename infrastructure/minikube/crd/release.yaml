apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crd
  namespace: flux-system
spec:
  interval: 1m
  releaseName: crd
  chart:
    spec:
      chart: ../../../infrastructure/base/crd
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system